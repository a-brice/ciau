services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./ciau/.env:/app/.env:ro
      - ./ciau/db.sqlite3:/app/db.sqlite3
      - ./ciau/uploads:/app/uploads
    restart: unless-stopped
